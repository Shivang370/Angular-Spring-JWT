import { AlphanumbersDirective } from './alphanumbers.directive';

describe('AlphanumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphanumbersDirective();
    expect(directive).toBeTruthy();
  });
});
